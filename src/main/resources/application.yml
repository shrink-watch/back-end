spring:
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/shrinkwatch?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: xhtm1234
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
    defer-datasource-initialization: true   # JPA 테이블 생성 후 data.sql 실행
    properties:
      hibernate:
        format_sql: true
  sql:
    init:
      mode: always   # data.sql 항상 실행

server:
  port: 8080